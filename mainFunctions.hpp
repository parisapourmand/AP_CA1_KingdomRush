#include "functions.hpp"
#include "rsdl.hpp"
#include <vector>
#include <string> 
#include <unistd.h>
using namespace std;

bool playLevels(Window &w, int level);
bool losePage(Window &w);
bool winPage(Window &w);